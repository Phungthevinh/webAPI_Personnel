services:
  app:
    build:
        context: .  
    ports:
      - 8080:8080
    # working_dir: /app
    environment:
      POSTGRES_USER: mangerPerson
      POSTGRES_PASSWORD: vinh2711
      POSTGRES_DB: mangerPerson
    env_file:
      - .env
    depends_on:
        - postgres


  postgres:
    image: postgis/postgis
    container_name: postgres_container
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: mangerPerson
      POSTGRES_PASSWORD: vinh2711
      POSTGRES_DB: mangerPerson
    ports:
        - "5432:5432"

volumes:
  pgdata: